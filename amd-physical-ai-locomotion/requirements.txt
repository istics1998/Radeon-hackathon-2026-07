# Python dependencies for AMD Radeon / ROCm locomotion RL.
#
# IMPORTANT: JAX itself is NOT pinned here. The ROCm build of JAX must be
# installed separately (see scripts/00_verify_rocm.sh and README.md), because
# the correct wheel/container depends on the ROCm version on your Radeon
# instance. Installing plain `jax` here would pull the CPU build and silently
# disable GPU acceleration.
#
# These are the framework-level deps that are backend-agnostic.
mujoco>=3.2.0
mujoco-mjx>=3.2.0
brax>=0.10.5
mujoco-playground>=0.0.4
# flax + optax back our from-scratch single-GPU PPO (src/train_jax_ppo.py).
# They ship as brax deps too, but we pin them since we import them directly.
flax>=0.8.0
optax>=0.2.0
ml_collections>=0.1.1
etils>=1.7.0
mediapy>=1.2.0
tensorboardX>=2.6
tqdm>=4.66
